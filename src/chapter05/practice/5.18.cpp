#include "include_header.h"

int main() {
	//do
	//	int v1, v2;
	//	cout << "Please enter two numbers to sum:";
	//	if (cin >> v1 >> v2)
	//		cout << "Sum is: " << v1 + v2 << endl;
	//while (cin);

	//do {} while (int ival = rand());

	//do {
	//	int ival = rand();
	//} while (ival);
}